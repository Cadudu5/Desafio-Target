#include<stdio.h>

int main(){
    //a = numeros impares comecando em 1, proximo numero sera 9.//

    //b = o numero dobra apartir do 2, potencias de base 2, proximo numero sera 128.

    /*c = um numero impar (da sequencia da questao 'A') eh somado ao anterior, comecando em 1
    o proximo numero sera 49*/

    /*d = o primero numero (4) eh multiplicado por 1, por 4, depois por 9, depois por 16.
    entre esses numeros que multiplicam o 4, aparece novamente a sequencia de numeros impares da
    questao 'A', o numero 3 entre 1 e 4, 5 entre 4 e 9, e 7 entre 9 e 16, assim o priximo numero
    sera 9 distante de 16, portando 25. Agora basta multiplicar esse valor por 4, resultando em 100.*/

    /*e = representa a sequencia de fibonacci, onde cada termo (apartir de 0) eh a soma dos dois
    termos anteriores, portanto o proximo numero sera o 13*/

    /*f = nao consegui encontrar um padrao */
}